import React, { Fragment } from 'react'
import { SectionTitle } from '../Typography'
const ContributionsCopy = ({ title, subtitle }) => (
  <Fragment>
    <SectionTitle reverse>
      {title} 44000 contributions to 3000 open source projects
    </SectionTitle>
  </Fragment>
)

export default ContributionsCopy
