import React from 'react'

const ContributionsCopy = ({ contributions, reposCount }) => (
  <p>
    Total contributions {contributions} to {reposCount} repos
  </p>
)

export default ContributionsCopy
