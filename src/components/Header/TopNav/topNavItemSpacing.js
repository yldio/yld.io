import { css } from 'styled-components'
import remcalc from 'remcalc'

const topNavItemSpacing = css`
  padding: ${remcalc(6)} ${remcalc(11)} ${remcalc(10)};
  margin: ${remcalc(4)};
`

export default topNavItemSpacing
